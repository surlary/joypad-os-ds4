# Joypad usb2usb — nRF52840 Kconfig overrides
#
# Applied on top of prj.conf when building APP_TYPE=usb2usb.
# Enables SPI for MAX3421E FeatherWing, disables Bluetooth.

# SPI driver disabled — MAX3421E uses direct NRF_SPI1 register access (ISR-safe)
CONFIG_SPI=n
CONFIG_BT=n
